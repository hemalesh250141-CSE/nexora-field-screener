import 'package:flutter/material.dart';

/// NEXORA Government/Forensic Design System Color Palette
/// Strict law-enforcement visual standard: High contrast, authoritative, zero decorative clutter.
class NexoraColors {
  NexoraColors._();

  // Primary Forensic Colors (Mandated)
  static const Color classicBlack = Color(0xFF111111);
  static const Color pureWhite = Color(0xFFFFFFFF);
  static const Color deepBrown = Color(0xFF3E2723);
  static const Color tacticalKhaki = Color(0xFFC3B091);

  // Surface & Structural Shades
  static const Color backgroundBlack = Color(0xFF0D0D0D);
  static const Color cardDark = Color(0xFF181818);
  static const Color cardElevated = Color(0xFF222222);
  static const Color tableRowAlt = Color(0xFF151515);
  static const Color borderSubtle = Color(0xFF2E2E2E);
  static const Color borderStrong = Color(0xFF4A4A4A);

  // Forensic State & Warning Colors
  static const Color presumptiveAmber = Color(0xFFD97706);
  static const Color inconclusiveGray = Color(0xFF718096);
  static const Color alertRed = Color(0xFFB91C1C);
  static const Color verifiedGreen = Color(0xFF047857);
  static const Color syncBlue = Color(0xFF1D4ED8);

  // Text Hierarchy
  static const Color textPrimary = Color(0xFFF7FAFC);
  static const Color textSecondary = Color(0xFFCBD5E0);
  static const Color textMuted = Color(0xFF718096);
  static const Color textInverse = Color(0xFF111111);
}
