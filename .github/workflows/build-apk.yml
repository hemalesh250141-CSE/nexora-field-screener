name: Build NEXORA Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-apk:
    name: Build release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Configure Gradle version
        working-directory: android
        shell: bash
        run: |
          sed -i 's#gradle-[0-9.]*-all.zip#gradle-8.14.3-all.zip#' gradle/wrapper/gradle-wrapper.properties
          echo "Configured Gradle wrapper:"
          cat gradle/wrapper/gradle-wrapper.properties

      - name: Verify Gradle version
        working-directory: android
        shell: bash
        run: |
          echo "Gradle wrapper:"
          grep distributionUrl gradle/wrapper/gradle-wrapper.properties
          chmod +x gradlew
          ./gradlew --version

      - name: Flutter doctor
        run: flutter doctor -v

      - name: Get dependencies
        run: flutter pub get

      - name: Build release APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Verify APK
        shell: bash
        run: |
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"

          if [ ! -f "$APK_PATH" ]; then
            echo "ERROR: APK was not generated."
            exit 1
          fi

          echo "APK successfully generated:"
          ls -lh "$APK_PATH"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: nexora-field-screener-release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error